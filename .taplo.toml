# Formatting for every TOML file, so an editor's "Even Better TOML" and CI agree on one layout.
[formatting]
align_entries = false
array_auto_collapse = true
column_width = 120
indent_string = "  "
reorder_keys = false
